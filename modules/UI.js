'use strict';

// getComments is a function, that gets an ID as an argument. it then return all the comments
// associated with the given path.
const getComments = (ID) =>{
  //do a DB query for the media comments
};